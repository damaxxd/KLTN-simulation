# SVC Model

Layers:
- BL (base layer)
- EL1, EL2, EL3

Mapping:
- Rsc → BL
- Rsp → EL

Effective rate:
Reff_s = min(Rsc, r_BL) + min(Rsp, r_EL_total)

Layer decoding:
Compare Reff_s with cumulative bitrate thresholds